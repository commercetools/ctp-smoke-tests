auth-test-vars {
    auth-url: "https://auth.sphere.io/oauth/token"
    auth-url: ${?AUTH_URL}
    client-id: ${?CLIENT_ID}
    client-secret: ${?CLIENT_SECRET}
    project-key: ${?PROJECT_KEY}
 }
 cornichon {}
 
boolean = true
port = 8080
adt {
	type = "adt-b"
	 b = 1
}
list = ["1", "20%"]
map {
	key = "value"
}
