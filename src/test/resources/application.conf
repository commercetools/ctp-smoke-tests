auth-test-vars {
    auth-url: "https://auth.sphere.io/oauth/token"
    auth-url: ${?AUTH_URL}
    client-id: ${CLIENT_ID}
    client-secret: ${CLIENT_SECRET}
    project-key: ${PROJECT_KEY}
 }
 cornichon {}
